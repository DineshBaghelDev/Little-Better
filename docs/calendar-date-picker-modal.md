# Calendar Date Picker Modal

- Moved the Calendar page date picker behind the header calendar icon.
- Kept the seven-day selector visible as the primary date control.
- Let the modal picker open directly to the selected date.
- Verified with `npm run typecheck` and `npm run lint`.
