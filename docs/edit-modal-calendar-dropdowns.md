# Edit Modal Calendar Dropdowns

- Moved transaction editing into a modal while keeping the Money add form inline.
- Added a reusable choice dropdown and used it for transaction type/payment and Calendar task time.
- Added the shared date picker to Calendar date selection.
- Made category dropdown content scroll so long category lists are reachable.
- Verified with `npm run typecheck` and `npm run lint`.
